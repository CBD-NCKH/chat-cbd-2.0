flask==2.2.5  # Đảm bảo tương thích tốt với Flask-Session
flask-cors==3.0.10  # Phiên bản ổn định và tương thích với Flask 2.x
Flask-Session==0.4.0  # Phiên bản mới nhất của Flask-Session
python-dotenv==0.21.0  # Đảm bảo tương thích tốt với các phiên bản Python mới
openai==0.27.6  # Phiên bản ổn định và tương thích
gunicorn==20.1.0  # Phiên bản ổn định
spacy==3.5.2  # Cần tương thích với mô hình ngôn ngữ tải về
pyvi==0.1.1  # Phiên bản ổn định của Pyvi
scikit-learn==0.24.2  # Tương thích với numpy < 1.25
numpy==1.24.2  # Cố định phiên bản để tương thích với scikit-learn và spacy
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.5.0/en_core_web_sm-3.5.0-py3-none-any.whl
gspread==5.7.3  # Đảm bảo hoạt động tốt với oauth2client
oauth2client==4.1.3  # Cố định phiên bản để tránh xung đột

